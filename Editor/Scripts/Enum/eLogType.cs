namespace ArchNet.Service.Console.Enum
{
    /// <summary>
    /// [SERVICE] - [ARCH NET] - [CONSOLE] : enum to refere all level of a log
    /// @author : LOUIS PAKEL
    /// </summary>
    public enum eLogType
    {
        NONE,
        LOG,
        ERROR,
        WARNING,
        ASSERT,
        EXCEPTION
    }
}
